declare module 'ketcher-standalone' {
  export class StandaloneStructServiceProvider {
    constructor();
    // This is not a complete type definition, but it's enough
    // to satisfy TypeScript for our use case.
  }
} 